# 2_TO_4_DECODER
Design a 2-to-4 decoder with an enable signal. When enable is high, the decoder should activate the output corresponding to the binary input. When enable is low, all outputs should be low. Implement using both dataflow and behavioral modeling styles.
